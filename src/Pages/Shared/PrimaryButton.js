

const PrimaryButton = ({ children }) => {
    return (

        <button className="bg-gradient-to-r from-indigo-500 btn btn-primary uppercase 
        text-white font-bold">{children}</button>

    );
};

export default PrimaryButton;